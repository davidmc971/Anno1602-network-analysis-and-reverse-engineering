{
  "name": "backend",
  "description": "Rust backend scripts",
  "scripts": {
    "dev": "cargo watch -q -B 1 -w crates/directplay-rs/src -w src -x 'run -q'",
    "start": "cargo run",
    "build": "cargo build",
    "lint": "cargo clippy",
    "format": "cargo fmt",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "David Alexander Pfeiffer",
  "license": "ISC"
}
